import Form, {Input, Button} from './form';
import PartialsComponent, {AlertStatus, BackgroundWrapper, Logo} from './partialsComponent';
import Text, {TextFont, Heading} from './text';

export {
    Form, Input, Button,
    PartialsComponent, AlertStatus, BackgroundWrapper, Logo,
    Text, TextFont, Heading
}

export default {
    Form, Input, Button,
    PartialsComponent, AlertStatus, BackgroundWrapper, Logo,
    Text, TextFont, Heading
}